# dd + Hono

A minimal Hono application running on the dd Vite worker adapter.

```bash
pnpm install
pnpm dev
```

Use `pnpm build` to create a deployable worker bundle and `pnpm typecheck` to
check the application.
