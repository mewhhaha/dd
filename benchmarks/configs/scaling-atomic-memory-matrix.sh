#!/usr/bin/env bash
set -euo pipefail

DD_BENCH_MATRIX_ISOLATES="1 2 4 8 16 32"
DD_BENCH_MATRIX_MEMORY_NAMESPACE_SHARDS="1 2 4 8 16 32 64"
DD_BENCH_MATRIX_KEY_MODES="same-shard cross-shard skewed-hotspot"
DD_BENCH_MATRIX_MODES="direct-write-memory-wide atomic-readwrite-memory-wide atomic-write-memory-wide"
DD_BENCH_MATRIX_OPT_IN=1

DD_BENCH_MODE="${DD_BENCH_MODE:-direct-write-memory-wide}"
DD_BENCH_REQUESTS="${DD_BENCH_REQUESTS:-4096}"
DD_BENCH_CONCURRENCY="${DD_BENCH_CONCURRENCY:-128}"
DD_BENCH_MIN_ISOLATES="${DD_BENCH_MIN_ISOLATES:-16}"
DD_BENCH_MAX_ISOLATES="${DD_BENCH_MAX_ISOLATES:-16}"
DD_BENCH_MAX_INFLIGHT="${DD_BENCH_MAX_INFLIGHT:-16}"
DD_BENCH_WIDE_KEY_SPACE="${DD_BENCH_WIDE_KEY_SPACE:-1024}"
DD_BENCH_MEMORY_NAMESPACE_SHARDS="${DD_BENCH_MEMORY_NAMESPACE_SHARDS:-16}"
DD_BENCH_MEMORY_KEY_MODE="${DD_BENCH_MEMORY_KEY_MODE:-cross-shard}"
DD_BENCH_VERIFY_TIMEOUT_MS="${DD_BENCH_VERIFY_TIMEOUT_MS:-20000}"

export DD_BENCH_MODE
export DD_BENCH_REQUESTS
export DD_BENCH_CONCURRENCY
export DD_BENCH_MIN_ISOLATES
export DD_BENCH_MAX_ISOLATES
export DD_BENCH_MAX_INFLIGHT
export DD_BENCH_WIDE_KEY_SPACE
export DD_BENCH_MEMORY_NAMESPACE_SHARDS
export DD_BENCH_MEMORY_KEY_MODE
export DD_BENCH_VERIFY_TIMEOUT_MS

cargo run -p runtime --bin bench_memory_storage --release
