--- a/00_crypto.js
+++ b/00_crypto.js
@@ -13,8 +13,8 @@
 // singletons (cppgc allocations can't run at snapshot-build time), the
 // structured-clone resurrection callback, and the small `deriveBits`
 // forwarder that gives the spec-mandated `Function.length === 2`.
-
-(function () {
+//
+const ddCrypto = (function () {
 const { core, primordials, internals } = __bootstrap;
 const {
   Crypto,
@@ -34,7 +34,9 @@
 const { createFilteredInspectProxy } = core.loadExtScript(
   "ext:deno_web/01_console.js",
 );
-const { kKeyObject } = internals;
+// dd does not embed deno_node, which normally installs this private symbol.
+// WebCrypto only needs a stable symbol shared by its own objects.
+const kKeyObject = SymbolFor("dd.crypto.kKeyObject");
 
 // op2-generated interface constructors expose the macro's internal
 // new-target signal (`_: bool`) as a formal parameter, giving the
@@ -298,3 +300,24 @@
   SubtleCrypto,
 };
 })();
+
+const {
+  Crypto,
+  CryptoKey,
+  cryptoKeyExportNodeKeyMaterial,
+  importCryptoKeySync,
+  SubtleCrypto,
+} = ddCrypto;
+
+function getCrypto() {
+  return ddCrypto.crypto;
+}
+
+export {
+  Crypto,
+  CryptoKey,
+  cryptoKeyExportNodeKeyMaterial,
+  getCrypto,
+  importCryptoKeySync,
+  SubtleCrypto,
+};
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -31,7 +31,7 @@
 path = "lib.rs"
 
 [dependencies.aes]
-version = "=0.8.3"
+version = "0.8.4"
 
 [dependencies.aes-gcm]
 version = "0.10"
--- a/subtle_generate_key.rs
+++ b/subtle_generate_key.rs
@@ -190,8 +190,11 @@
       "ML-DSA-44" => Self::MlDsa(0, canonical),
       "ML-DSA-65" => Self::MlDsa(1, canonical),
       "ML-DSA-87" => Self::MlDsa(2, canonical),
-      _ if let Some(variant) = crate::slhdsa::variant_from_name(&canonical) => {
-        Self::SlhDsa(variant)
+      _ if crate::slhdsa::variant_from_name(&canonical).is_some() => {
+        Self::SlhDsa(
+          crate::slhdsa::variant_from_name(&canonical)
+            .expect("SLH-DSA variant was checked"),
+        )
       }
       _ => Self::Unknown(canonical),
     })
--- a/subtle_get_public_key.rs
+++ b/subtle_get_public_key.rs
@@ -109,9 +109,9 @@
         RawKeyData::Raw(derived.public_key.into_boxed_slice()),
       ))
     }
-    _ if let Some(variant) =
-      crate::slhdsa::variant_from_name(algorithm_name) =>
-    {
+    _ if crate::slhdsa::variant_from_name(algorithm_name).is_some() => {
+      let variant = crate::slhdsa::variant_from_name(algorithm_name)
+        .expect("SLH-DSA variant was checked");
       validate_public_key_usages(&usages, &["verify"])?;
       let public_key = crate::slhdsa::public_from_private(
         variant,
--- a/subtle_sign.rs
+++ b/subtle_sign.rs
@@ -169,7 +169,9 @@
           context: context_bytes,
         })
       }
-      _ if let Some(variant) = crate::slhdsa::variant_from_name(canonical) => {
+      _ if crate::slhdsa::variant_from_name(canonical).is_some() => {
+        let variant = crate::slhdsa::variant_from_name(canonical)
+          .expect("SLH-DSA variant was checked");
         let context_bytes = match maybe_obj {
           Some(o) => read_optional_buffer_source(
             scope,
--- a/subtle_verify.rs
+++ b/subtle_verify.rs
@@ -163,7 +163,9 @@
           context: context_bytes,
         })
       }
-      _ if let Some(variant) = crate::slhdsa::variant_from_name(canonical) => {
+      _ if crate::slhdsa::variant_from_name(canonical).is_some() => {
+        let variant = crate::slhdsa::variant_from_name(canonical)
+          .expect("SLH-DSA variant was checked");
         let context_bytes = match maybe_obj {
           Some(o) => read_optional_buffer_source(
             scope,
